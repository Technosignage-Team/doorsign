import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.doorsign.app',
  appName: 'everest-room-booking-dashboard',
  webDir: 'dist'
};

export default config;
